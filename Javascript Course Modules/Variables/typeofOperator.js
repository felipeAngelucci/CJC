let newVariable = 'Playing around with typeof.';
/*
the typeof operator checks the data type of a variable and returns a string describing the data type.
*/
console.log(typeof newVariable);
newVariable = 1;
console.log(typeof newVariable);
// typeof is a unary operator and not a method or variable. All the unary operators in JavaScript are written in lowercase.
